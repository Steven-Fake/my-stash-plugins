name: Gallery Organizer
# requires: PythonDepManager
description: A plugin to organize some galleries by my own rules.
version: 1.0.14
exec:
  - python
  - "{pluginDir}/main.py"
interface: raw
tasks:
  - name: Add date for Galleries
    description: Extract the date from the file path and add it to the metadata. / 从文件路径中提取日期并添加到元信息中
    defaultArgs:
      mode: galleries_date
  - name: Add title for Galleries
    description: Extract the title from the file path and add it to the metadata. / 从文件路径中提取标题并添加到元信息中
    defaultArgs:
      mode: galleries_title
  - name: Add performers for Galleries
    description: Extract performers from the file path and add it to the metadata. / 从文件路径中作者并添加到元信息中
    defaultArgs:
      mode: galleries_performers
  - name: Add metadata for JVID galleries
    description: Add metadata for JVID galleries. / 为JVID相册添加元信息
    defaultArgs:
      mode: add_jvid_metadata
  - name: Add metadata for XIUREN series galleries
    description: Add metadata for XIUREN series galleries. / 为XIUREN系列相册添加元信息
    defaultArgs:
      mode: add_xiuren_metadata
  - name: Sort urls
    description: Merge the same url. / 合并实质相同的URL
    defaultArgs:
      mode: galleries_urls